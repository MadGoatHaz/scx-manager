# Build artifacts
build/
target/
*.o
*.a
*.so
*.so.*

# Rolling dev state (not part of the AUR package)
DEV_LOG.md
MASTER_LOG.md
plans/
.kilo/

# Generated AUR source tarball (recreate: git archive, see packaging/)
packaging/*/scx-manager-*.tar.gz

# AUR makepkg build artifacts
packaging/*/pkg/
packaging/*/src/
packaging/*/*.log
packaging/*/*.sig
packaging/*/*.pkg.tar.*
